/* Which side of the table you are. proto set this inline in the page;
   it is a file here so the build stays declarative. The hall will set it
   per table once going live is wired up. */
window.__ROLE__ = 'gm';
